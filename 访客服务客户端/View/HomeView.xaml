<UserControl x:Class="Visitor.View.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Visitor.View"
             xmlns:localh="clr-namespace:Visitor.Helper"
             xmlns:localct="clr-namespace:Visitor.Converter"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <!--<ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="465px"/>-->
        </Grid.ColumnDefinitions>
        <localct:VisitingControl x:Name="vc"/>
        <!--<GridSplitter Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Stretch" Width="3">
            <GridSplitter.Background>
                <SolidColorBrush Color="{DynamicResource Primary900}" />
            </GridSplitter.Background>
        </GridSplitter>-->
        <!--<localct:ACSRecordControl Grid.Column="2"/>-->
    </Grid>
</UserControl>
